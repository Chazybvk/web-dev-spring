console.log("Hello")
let num = 5;
var str = "Hello";
// var str = ` ${str} `
1/0
var bool = true;
let arr = new Array(1, 2, 3);
console.log(arr)

let l = Array(4, 0)
console.log(Boolean(l.length) && false )
let x = -1;

l.forEach(
    (el) => { обработка}
)
(условие) ? true : false;

if (x > 0)
  console.log("x is greater than 0");
else 
  console.log( "x is less than or equal to 0")


  function myFunction($arr_) {
    var_dump($arr_);
  }

  myFunction(arr); 
